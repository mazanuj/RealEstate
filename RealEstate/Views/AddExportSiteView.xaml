<Window x:Class="RealEstate.Views.AddExportSiteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AddExportSiteView" Height="170" Width="400">
    <Grid Background="White">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Grid.Row="0" Margin="3">Название сайта: </TextBlock>
            <TextBox  Grid.Column="1" Grid.Row="0" Margin="3" Name="Title" />

            <TextBlock Grid.Column="0" Grid.Row="1" Margin="3">URL:</TextBlock>
            <TextBox Grid.Column="1" Grid.Row="1" Margin="3" Name="Url" />

            <Button Name="Create" Grid.Row="2" Grid.ColumnSpan="2" Content="Создать" Margin="5" 
                    VerticalAlignment="Bottom" HorizontalAlignment="Right" />
        </Grid>
    </Grid>
</Window>
